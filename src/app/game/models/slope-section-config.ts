import type { Color, ImageSource } from 'excalibur';

export interface SlopeSectionConfig {
    texture: ImageSource;
    labelColor: Color;
    dividerColor: Color;
    particlesColor: Color;
    profileColor: Color;
}
